Drop your app icons here as PNG (transparent background), ~256x256 px.
Filenames (exact):
  smart-calculator.png
  proforma-invoice.png
  order-dispatch.png
  customer-database.png
SVG also works if you rename the reference; PNG is simplest.
Until a file exists, a built-in placeholder icon is shown automatically.
